package Strange_Bank_Problem;

/* TODO: Try creating a system like this:
 * A Bank class, which has an integer variable money.
 * And Client class, a subclass of Thread class,
 * which in infinite loop takes $1000 loan from bank (decrease money) and immediately returns (increase money).
 */
public class Bank {}
